version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: clean-arch-postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: clean_arch
    ports:
      - "5432:5432"